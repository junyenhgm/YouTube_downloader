# YouTube_downloader

### Dependency

Install `pytube`
```
pip install pytube
```

Install `moviepy`
```
pip install moviepy
```
\
Go for [ffmpeg](https://ffmpeg.org/download.html) if file converti is needed.
